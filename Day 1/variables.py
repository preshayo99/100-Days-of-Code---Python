# name = input('What is your name?: ')
# print('Hello ' + name + '!')

name = 'Precious'
print('There are '+ str(len(name)) + ' letters in the name ' + name)
#because len() returns an integer, we need to convert it to a string using str()

username = input('What is your name?: ')
length = len(username)
print('Hello ' + username + '! You have ' + str(length) + ' letters in your name.')